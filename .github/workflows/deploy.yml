name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:

      - name: Run Deployment Script
        run: |
          cd /var/www/library && ./deploy_back.sh
